let estatura = 168;

let altura_m = 1.68;

let peso = 65.82;

let peso_red = peso.toFixed(1);

let altura_m_red = altura_m.toPrecision(5);

let x = Number.MAX_VALUE + 1 === Number.MAX_VALUE;